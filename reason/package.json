{
  "name": "learning",
  "version": "0.1.0",
  "description": "Learning ReasonML",
  "license": "MIT",
  "esy": {
    "build": "dune build -p #{self.name}",
    "buildDev": "refmterr dune build --promote-install-files --root . --only-package #{self.name}"
  },
  "dependencies": {
    "ocaml": "4.10.x",
    "@opam/dune": ">=2.6",
    "@esy-ocaml/reason": ">= 3.6.0 < 4.0.0"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "refmterr": "*"
  }
}
